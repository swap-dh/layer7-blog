services:
  blog:
    container_name: layer7-blog
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "2368:2368"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=2368
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
